<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Needed for complication updates when device is idle -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="AetherSmart"
        android:theme="@android:style/Theme.DeviceDefault">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tile Service -->
        <service
            android:name=".tile.AetherTileService"
            android:exported="true"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
        </service>

        <!-- ADB config push (no typing on watch) -->
        <receiver
            android:name=".data.ConfigBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.aethersmart.wearos.SET_CONFIG" />
            </intent-filter>
        </receiver>

        <!-- Complication Service -->
        <service
            android:name=".complication.PowerComplicationService"
            android:exported="true"
            android:permission="com.google.android.wearable.complications.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.watchface.complications.datasource.ComplicationDataSourceService" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.watchface.complications.datasource.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,LONG_TEXT,RANGED_VALUE" />
            <meta-data
                android:name="androidx.wear.watchface.complications.datasource.UPDATE_PERIOD_SECONDS"
                android:value="900" />
        </service>

    </application>
</manifest>
